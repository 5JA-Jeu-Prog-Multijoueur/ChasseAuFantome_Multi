using UnityEngine;

public class ManagerMusiqueLobbyFin : MonoBehaviour
{
    public AudioSource audioSourceLobbyFin;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        audioSourceLobbyFin.Play();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
